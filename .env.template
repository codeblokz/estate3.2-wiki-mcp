# .env — Estate 3.0 Wiki MCP Configuration
# DO NOT COMMIT THIS FILE
# Copy to project root and populate with your values

# Your Tailscale hostname (find via: tailscale status --json | jq '.Self.DNSName')
TAILSCALE_HOSTNAME=tower.tail12345.ts.net

# Generate with: openssl rand -hex 32
MCP_CLIENT_SECRET=your-secret-here

# Generate with: openssl rand -hex 32
MCP_BEARER_TOKEN=your-bearer-token-here
